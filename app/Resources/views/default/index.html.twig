{% extends 'base.html.twig' %}

{% block body %}
    {# If there are no feeds, show this: #}
    <div class="no-feeds">
        <div class="text">{% trans %}no_feeds_available{% endtrans %}</div>
        <a href="#" onclick="showSection('{{ path('asyncAddFeeds') }}')">{% trans %}add_feeds{% endtrans %}</a>
    </div>

    {# else #}


{% endblock %}